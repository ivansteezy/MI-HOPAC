<Page x:Class="MI_HOPAC.Views.Notificaciones"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MI_HOPAC"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Dashboard"
      Background="White">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="ListaNotifiaciones" Focusable="True" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" 
                  ItemsSource="{Binding Notificacion}" ColumnWidth="*" FontSize="20"
                  Background="Transparent" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mensaje" Binding="{Binding Nombre}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
